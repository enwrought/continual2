{
  "name": "lib",
  "version": "0.0.1",
  "description": "Shared code for diary-app",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --skipLibCheck",
    "clean": "rimraf build",
    "tslint": "tslint ./**/*.ts{,x}",
    "prepare": "npm run clean && npm run build"
  },
  "author": "Bryant Lin",
  "license": "MIT",
  "devDependencies": {
    "@types/dotenv": "^4.0.3",
    "@types/jest": "^23.0.0",
    "@types/node": "^10.3.0",
    "dotenv": "^6.0.0",
    "jest": "^23.1.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.15.0",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-react": "^3.6.0",
    "typescript": "^3.4.3",
    "typescript-eslint-parser": "^16.0.0"
  }
}
